# v0.1.1 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** v0.1.1 の数値入力改善と、イベント未設定時でも設定タブでアプリ状態を確認できる導線を実装する。

**Architecture:** `src/components/NumberField.tsx` をアプリ内の共通数値入力として強化し、管理画面のローカル数値入力をこの部品へ寄せる。設定画面はイベントIDを任意値として扱い、アプリ状態は常に表示し、イベント単位の操作だけを無効化する。

**Tech Stack:** React, TypeScript, Vite, Vitest, Testing Library, Dexie, TailwindCSS

---

## File Structure

- Modify: `src/components/NumberField.tsx`
  - 親からは `number` の `value` と `onChange(number)` を受け取るまま、フォーカス中だけ文字列の入力ドラフトを持つ。
- Modify: `src/components/BasicComponents.test.tsx`
  - `NumberField` のゼロ表示クリア、入力反映、空欄 blur 正規化を検証する。
- Modify: `src/pages/ManagementPage.tsx`
  - ローカル `NumberInput` を `NumberField` の薄いラッパーに置き換える。
- Modify: `src/pages/ManagementPage.test.tsx`
  - 管理画面の数値入力で初期 `0` が残らないことを検証する。
- Modify: `src/App.tsx`
  - イベント未選択でも `SettingsPage` を描画する。
- Modify: `src/App.test.tsx`
  - 空DBで設定タブを押すと設定画面とアプリ状態が出ることを検証する。
- Modify: `src/pages/SettingsPage.tsx`
  - `eventId` 未指定時はアプリ状態のみ使えるようにし、CSV出力とリセットを無効化する。
- Modify: `src/pages/SettingsPage.test.tsx`
  - イベント未選択時のボタン無効化を検証する。

---

### Task 1: 共通 NumberField の入力ドラフト対応

**Files:**
- Modify: `src/components/NumberField.tsx`
- Modify: `src/components/BasicComponents.test.tsx`

- [ ] **Step 1: Write the failing NumberField tests**

Add these imports to `src/components/BasicComponents.test.tsx` if they are not already present:

```tsx
import { render, screen, waitFor } from "@testing-library/react";
```

Add this test inside `describe("basic components", () => { ... })`:

```tsx
it("lets zero-valued number fields be replaced without keeping the leading zero", async () => {
  const onChange = vi.fn();

  render(<NumberField label="価格" value={0} min={0} onChange={onChange} />);

  const input = screen.getByRole("spinbutton", { name: "価格" });

  await userEvent.click(input);
  expect(input).toHaveValue(null);

  await userEvent.type(input, "1200");

  expect(onChange).toHaveBeenLastCalledWith(1200);
  expect(input).toHaveValue(1200);
});

it("normalizes an empty number field on blur", async () => {
  const onChange = vi.fn();

  const { rerender } = render(
    <NumberField label="取り置き数" value={0} min={0} onChange={onChange} />,
  );

  const input = screen.getByRole("spinbutton", { name: "取り置き数" });

  await userEvent.click(input);
  await userEvent.tab();

  expect(onChange).toHaveBeenLastCalledWith(0);

  rerender(<NumberField label="取り置き数" value={0} min={0} onChange={onChange} />);

  await waitFor(() => {
    expect(screen.getByRole("spinbutton", { name: "取り置き数" })).toHaveValue(0);
  });
});
```

- [ ] **Step 2: Run the focused component test and verify it fails**

Run:

```bash
npm run test -- src/components/BasicComponents.test.tsx
```

Expected: FAIL. The first new test should show that the input still contains `0` after focus or produces a leading-zero editing experience.

- [ ] **Step 3: Implement NumberField draft input behavior**

Replace `src/components/NumberField.tsx` with:

```tsx
import { useEffect, useState } from "react";

interface NumberFieldProps {
  label: string;
  value: number;
  min?: number;
  max?: number;
  onChange: (value: number) => void;
}

export function NumberField({
  label,
  value,
  min,
  max,
  onChange,
}: NumberFieldProps) {
  const [draftValue, setDraftValue] = useState<string | null>(null);
  const displayValue = draftValue ?? String(value);
  const decrementDisabled = min !== undefined && value <= min;
  const incrementDisabled = max !== undefined && value >= max;

  useEffect(() => {
    if (draftValue === null) {
      return;
    }

    const parsedDraft = Number(draftValue);
    if (draftValue !== "" && Number.isFinite(parsedDraft) && parsedDraft === value) {
      setDraftValue(String(value));
    }
  }, [draftValue, value]);

  function clampValue(nextValue: number): number {
    const minApplied = min !== undefined ? Math.max(min, nextValue) : nextValue;
    return max !== undefined ? Math.min(max, minApplied) : minApplied;
  }

  function commitValue(nextValue: number) {
    onChange(clampValue(nextValue));
  }

  function handleFocus() {
    setDraftValue(value === 0 ? "" : String(value));
  }

  function handleChange(nextDraftValue: string) {
    setDraftValue(nextDraftValue);
    if (nextDraftValue === "") {
      return;
    }

    const nextValue = Number(nextDraftValue);
    if (Number.isFinite(nextValue)) {
      commitValue(nextValue);
    }
  }

  function handleBlur() {
    if (draftValue === "" || draftValue === null || !Number.isFinite(Number(draftValue))) {
      commitValue(min ?? 0);
      setDraftValue(null);
      return;
    }

    commitValue(Number(draftValue));
    setDraftValue(null);
  }

  return (
    <label className="block">
      <span className="text-sm font-bold">{label}</span>
      <div className="mt-1 grid grid-cols-[3rem_1fr_3rem] items-center gap-2">
        <button
          type="button"
          aria-label={`${label}を減らす`}
          disabled={decrementDisabled}
          className="min-h-12 rounded-md border bg-white text-xl font-bold disabled:opacity-40"
          onClick={() => commitValue(value - 1)}
        >
          -
        </button>
        <input
          type="number"
          min={min}
          max={max}
          value={displayValue}
          onBlur={handleBlur}
          onChange={(event) => handleChange(event.currentTarget.value)}
          onFocus={handleFocus}
          className="min-h-12 rounded-md border px-3 text-center text-lg font-bold"
        />
        <button
          type="button"
          aria-label={`${label}を増やす`}
          disabled={incrementDisabled}
          className="min-h-12 rounded-md border bg-white text-xl font-bold disabled:opacity-40"
          onClick={() => commitValue(value + 1)}
        >
          +
        </button>
      </div>
    </label>
  );
}
```

- [ ] **Step 4: Run the focused component test and verify it passes**

Run:

```bash
npm run test -- src/components/BasicComponents.test.tsx
```

Expected: PASS for `BasicComponents.test.tsx`.

- [ ] **Step 5: Commit Task 1**

```bash
git add src/components/NumberField.tsx src/components/BasicComponents.test.tsx
git commit -m "fix: improve shared number field editing"
```

---

### Task 2: 管理画面の数値入力を共通 NumberField に寄せる

**Files:**
- Modify: `src/pages/ManagementPage.tsx`
- Modify: `src/pages/ManagementPage.test.tsx`

- [ ] **Step 1: Write the failing management integration test**

Add this test inside `describe("ManagementPage", () => { ... })` in `src/pages/ManagementPage.test.tsx`:

```tsx
it("replaces zero in management number inputs instead of appending after it", async () => {
  render(<ManagementPage database={database} initialSection="products" />);

  await userEvent.click(screen.getByRole("button", { name: "頒布物を追加" }));

  const priceInput = screen.getByRole("spinbutton", { name: "価格" });
  await userEvent.click(priceInput);
  await userEvent.type(priceInput, "1200");

  expect(priceInput).toHaveValue(1200);
});
```

- [ ] **Step 2: Run the focused management test and verify it fails**

Run:

```bash
npm run test -- src/pages/ManagementPage.test.tsx
```

Expected: FAIL before `ManagementPage` uses the improved shared number input.

- [ ] **Step 3: Import NumberField and replace the local number input implementation**

In `src/pages/ManagementPage.tsx`, add:

```tsx
import { NumberField } from "../components/NumberField";
```

Replace the local `NumberInput` function with:

```tsx
function NumberInput({
  label,
  onChange,
  value,
}: {
  label: string;
  onChange: (value: number) => void;
  value: number;
}) {
  return <NumberField label={label} value={value} min={0} onChange={onChange} />;
}
```

- [ ] **Step 4: Run the focused management test and verify it passes**

Run:

```bash
npm run test -- src/pages/ManagementPage.test.tsx
```

Expected: PASS for `ManagementPage.test.tsx`.

- [ ] **Step 5: Commit Task 2**

```bash
git add src/pages/ManagementPage.tsx src/pages/ManagementPage.test.tsx
git commit -m "fix: reuse shared number input in management"
```

---

### Task 3: イベント未選択でも設定タブを表示する

**Files:**
- Modify: `src/App.tsx`
- Modify: `src/App.test.tsx`
- Modify: `src/pages/SettingsPage.tsx`
- Modify: `src/pages/SettingsPage.test.tsx`

- [ ] **Step 1: Write the failing App navigation test**

Add this test inside `describe("App", () => { ... })` in `src/App.test.tsx`:

```tsx
it("shows settings app state when no event is registered", async () => {
  render(<App database={database} />);

  await userEvent.click(screen.getByRole("button", { name: "設定" }));

  expect(await screen.findByRole("heading", { name: "設定" })).toBeInTheDocument();
  expect(screen.getByRole("heading", { name: "アプリ状態" })).toBeInTheDocument();
  expect(screen.getByText("イベント未選択")).toBeInTheDocument();
});
```

- [ ] **Step 2: Write the failing SettingsPage no-event test**

Add this test inside `describe("SettingsPage", () => { ... })` in `src/pages/SettingsPage.test.tsx`:

```tsx
it("keeps event-scoped settings actions disabled without a selected event", async () => {
  render(<SettingsPage database={database} eventId={undefined} />);

  expect(screen.getByRole("heading", { name: "アプリ状態" })).toBeInTheDocument();
  expect(screen.getByText("イベント未選択")).toBeInTheDocument();

  expect(screen.getByRole("button", { name: "売上サマリーCSV" })).toBeDisabled();
  expect(screen.getByRole("button", { name: "売上詳細CSV" })).toBeDisabled();
  expect(screen.getByRole("button", { name: "頒布物移動CSV" })).toBeDisabled();
  expect(screen.getByRole("button", { name: "経費CSV" })).toBeDisabled();
  expect(screen.getByRole("button", { name: "収支CSV" })).toBeDisabled();
  expect(screen.getByRole("button", { name: "リセット確認へ" })).toBeDisabled();
});
```

- [ ] **Step 3: Run the focused settings tests and verify they fail**

Run:

```bash
npm run test -- src/App.test.tsx src/pages/SettingsPage.test.tsx
```

Expected: FAIL because `App` does not render `SettingsPage` without an event, and `SettingsPage` still treats the fallback `event-1` as a target.

- [ ] **Step 4: Render SettingsPage without requiring an event**

In `src/App.tsx`, replace:

```tsx
{screen === "settings" && effectiveSelectedEventId !== null && (
  <SettingsPage database={database} eventId={effectiveSelectedEventId} />
)}
```

with:

```tsx
{screen === "settings" && (
  <SettingsPage database={database} eventId={effectiveSelectedEventId ?? undefined} />
)}
```

- [ ] **Step 5: Make SettingsPage eventId optional without a fallback target**

In `src/pages/SettingsPage.tsx`, change the destructuring from:

```tsx
eventId = "event-1",
```

to:

```tsx
eventId,
```

Then replace the event-scoped derived values with:

```tsx
const selectedEvent = eventId
  ? events.find((event) => event.id === eventId)
  : undefined;
const selectedEventId = selectedEvent?.id;
const eventTargetLabel = selectedEvent?.name ?? "イベント未選択";
const eventSales = selectedEventId
  ? sales.filter((sale) => sale.eventId === selectedEventId)
  : [];
const eventExpenses = selectedEventId
  ? expenses.filter((expense) => expense.eventId === selectedEventId)
  : [];
const eventStats = selectedEventId
  ? calculateEventStats({ eventId: selectedEventId, sales, expenses })
  : calculateEventStats({ eventId: "", sales: [], expenses: [] });
const hasSelectedEvent = selectedEventId !== undefined;
const canUseEventExports = csvLoaded && hasSelectedEvent;
const canResetSelectedEvent = hasSelectedEvent && !resetPending;
```

For each CSV button handler, add this guard before downloading:

```tsx
if (!canUseEventExports || !selectedEventId) {
  return;
}
```

Use `selectedEventId` in filenames:

```tsx
filename: `sales-summary-${selectedEventId}.csv`,
```

Apply the same pattern to all five CSV filenames.

Replace target labels with `eventTargetLabel`:

```tsx
<ScreenTitle subtitle={eventTargetLabel}>設定</ScreenTitle>
```

```tsx
対象: {eventTargetLabel}
```

Disable CSV buttons with:

```tsx
disabled={!canUseEventExports}
```

Keep reset guarded with:

```tsx
async function handleResetSelectedEvent() {
  if (!selectedEvent || resetPending) {
    return;
  }
```

- [ ] **Step 6: Run focused settings tests and verify they pass**

Run:

```bash
npm run test -- src/App.test.tsx src/pages/SettingsPage.test.tsx
```

Expected: PASS for `App.test.tsx` and `SettingsPage.test.tsx`.

- [ ] **Step 7: Commit Task 3**

```bash
git add src/App.tsx src/App.test.tsx src/pages/SettingsPage.tsx src/pages/SettingsPage.test.tsx
git commit -m "fix: show settings app state without event"
```

---

### Task 4: 全体検証

**Files:**
- Verify all modified source and test files.

- [ ] **Step 1: Run all tests**

Run:

```bash
npm run test
```

Expected: PASS.

- [ ] **Step 2: Run lint**

Run:

```bash
npm run lint
```

Expected: PASS with zero warnings.

- [ ] **Step 3: Run build**

Run:

```bash
npm run build
```

Expected: PASS and Vite build output under `dist/`.

- [ ] **Step 4: Inspect final git diff**

Run:

```bash
git status --short
git diff --stat
```

Expected: only v0.1.1 implementation files are changed, with no generated build output staged.

- [ ] **Step 5: Commit verification follow-up if needed**

If verification required a small fix, commit it with:

```bash
git add src/components/NumberField.tsx src/components/BasicComponents.test.tsx src/pages/ManagementPage.tsx src/pages/ManagementPage.test.tsx src/App.tsx src/App.test.tsx src/pages/SettingsPage.tsx src/pages/SettingsPage.test.tsx
git commit -m "test: verify v0.1.1 behavior"
```

If no files changed during verification, do not create an empty commit.

---

## Self-Review

- Spec coverage: Task 1 and Task 2 cover all numeric input behavior. Task 3 covers Settings visibility without an event and disables event-scoped actions. Task 4 covers verification.
- 未確定項目の確認: 実装者が判断に迷う空欄や後回し指示は残していない。
- Type consistency: `NumberField` keeps `value: number`, `onChange: (value: number) => void`, optional `min` and `max`; `SettingsPage` keeps `eventId?: string`.
